from setuptools import setup

setup(
    name='ppac',
    version='0.1',
    description='Project Planning as Code',
    author='Matthias Rieck',
    author_email='matthias.rieck@icloud.com',
    packages=['ppac'],  # same as name
    install_requires=[],  # external packages as dependencies
)
